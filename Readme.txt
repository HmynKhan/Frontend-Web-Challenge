to clone use git clone gitrepo link (git clone https://github.com/BBertt/globalwatch.git)
install dpeenuce using npm i or npm install
other depedncy install like mui install (npm install @mui/material @emotion/react @emotion/styled or yarn add @mui/material @emotion/react @emotion/styled);

to push code on github use these comands:

cd /path/to/your/project
git init
git add .
git commit -m "First commit"
git remote add origin https://github.com/HmynKhan/Frontend-Web-Challenge.git
git push -u origin main
